package hr.fer.oprpp1.custom.scripting.lexer;

/**
 * Enum for different token types.
 * @author renat
 *
 */
public enum TokenType {
	EOF, VARIABLE, CONSTANT_INTEGER, CONSTANT_DOUBLE, STRING, FUNCTION, OPERATOR, TAG_NAME, CHANGE_STATE
}
